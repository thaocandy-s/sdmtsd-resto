import { notFound } from "next/navigation";

// Contact feature hidden — page returns 404. Restore from git history
// (client form using ./_components/ContactForm) if re-enabled later.
export default function ContactPage() {
  notFound();
}
